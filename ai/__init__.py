"""
PyOS NOVA — AI package
=======================
All artificial-intelligence subsystems for PyOS NOVA.

Modules:
    engine   Tiered LLM inference: llama-cpp-python → NanoLLM → RAG.
    gpu      GPU backend detection and llama-cpp install helpers.
    agent    Tool-use agent that executes OS operations with approval.
    advisor  Proactive background health advisor daemon.
    memory   Persistent contextual memory stored in the SOS.
    reviewer Static AST analysis + AI code review.
    agents   Multi-agent system with SOS message bus.
"""
